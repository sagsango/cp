/*
      lets j, j+1, .. n-1 has been romved then
      0, 1, .... i has been removed
      
      cost = ( no of 1s between i to j  )*2 + (n-j)*1 + (i+1)*1
      minimize this value.
      
      O(n)
*/
class Solution {
public:
    int minimumTime(string s) {
        
    }
};
