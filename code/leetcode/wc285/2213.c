class Solution {
public:
    vector<int> longestRepeating(string s, string queryCharacters, vector<int>& queryIndices) {
        /*
            :)
            
            set<pair<int,int>> segments[26];
            
            now for segemnet[i] = <left_starting_index,right_starting_index>
            
            you have to only update these segmnets and merge them max 2 when you insert new one.
        */
        
    }
};
