// use all_flag = 0, and BIT, value at curr index = BIT_query_for_value_at_index ^ all_flag.
class Bitset {
public:
    Bitset(int size) {
        
    }
    
    void fix(int idx) {
        
    }
    
    void unfix(int idx) {
        
    }
    
    void flip() {
        
    }
    
    bool all() {
        
    }
    
    bool one() {
        
    }
    
    int count() {
        
    }
    
    string toString() {
        
    }
};

/**
 * Your Bitset object will be instantiated and called as such:
 * Bitset* obj = new Bitset(size);
 * obj->fix(idx);
 * obj->unfix(idx);
 * obj->flip();
 * bool param_4 = obj->all();
 * bool param_5 = obj->one();
 * int param_6 = obj->count();
 * string param_7 = obj->toString();
 */
